笔记：

三层架构
泛型
集合框架


ArrayList: 数组
插入删除效率低，需要做移位操作。
遍历和随机访问效率较高。

LinkedList: 链表
插入和删除效率高，
查询效率低。


map  不在Collection下面
key-value
key唯一，key如果重复，值覆盖。

通过equals和hashcode两个方法来判断是否是同一个对象。

//自己创建的对象，如果使用来作为TreeMap的key则需要变成比较器对象
//1.实现Comparable接口
//2.调用TreeMap的Comparator构造方法。

set
HashSet比TreeSet效率更高。
因为TreeSet需要维护元素的次序。只有需要使用排序的set时才使用TreeSet。

栈：先进后出
队列：先进先出

迭代集合时尽量不要删除集合元素。

帮助类：Collections












