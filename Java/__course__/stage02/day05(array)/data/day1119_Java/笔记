复习+数组
数组：
例子：有12可乐需要一次搬运，塑料筐——筐里有一格一格格子，把可乐放到格子里，我们不自觉对这个格子产生编号（序列号）
概念：引用数据类型（内存分布，周一晚上讲），容器——存放东西，塑料筐（数组），编号（数组中的下标，计算机是0开始），通过数组的下标可以获
取到下标对应的数据[数组的元素]（塑料筐中可乐），增删改查都可以通过这个下标去操作；无论是引用数据类型/基本数据类型都能存放到数组中，但
有个前提条件，这一个数组中的数据类型必须提前定义，而且必须优先定义好数组的长度（一旦定义好就无法修改）




数组的声明由几种方式：

1，String []a = new String[length];再赋值

a[0]=?;.......

2,new完就直接初始化：

String []a = new String[]{?,?...};

3.你甚至不必new：

String []a = {?,?....}同样没问题。