1.在HTML上
添加
3个段落
3个标题(内容任意)
分别用 类,id
第一个段落字体设置成，红色，加粗，下划线
第二个段落将字体设置，40px
第三个段落将字体设置成:红色,加粗,下划线,40px
给标题设置成：字体大小为50px,背景颜色蓝色,字体居中

思考？
1.如果对同一个标签，设置不同的选择器(id,类,标签选择器)，
并且设置的属性(color)一样，但属性值不同该听谁的？
优先级从大到小的顺序为:
<p>111</p>
id 红色
class 绿色
p 黄色
id选择器>类选择器>标记选择器

2.如果对同一个标签，设置不同的选择器(id,类,标签选择器),
               但每个选择器设置的属性不一样(color,font-size,text-decoration),
               又会出现怎样的结果呢？
    <p>222</p>
    效果叠加
注意:id选择器 集合的写法不起作用

